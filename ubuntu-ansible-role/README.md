# Singularity recipe using the ansible role marvel-nccr.zeopp

## Todo
- depend on a specific version of the role
- note that now everything is built into /tmp of the HOST machine, check if
  this is ok
